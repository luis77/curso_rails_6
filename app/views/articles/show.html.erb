<div class="large-container center-block">
	<h1><%=@article.title%></h1>
	<div>
		<p>Escrito por: <strong><%=@article.user.email%></strong></p>
		<% if current_user.avatar.attached?%>
			<%=image_tag url_for(current_user.avatar), height:30%>
		<%end%>
	</div>
	<div>
		<%= @article.content %><!--obtiene el texto contenido enriquecido de tu tabla correspondiente -->
	</div>

	<div class="margin-top">
		<p class="label">Tags</p>
		<% @article.categories.each do |category|%>
			<span class="badge badge-pill padding" style="background-color: <%=category.color%>">
				<%= link_to category.title, category, class:"text-white"%>
			</span>
		<% end%>
	</div>

	<div>
   <a href="<%=edit_article_path(@article)%>" >Editar</a>	
		<%=link_to "Eliminar el articulo", @article, method: :delete%>
	</div>
</div>